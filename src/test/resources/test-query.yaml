---
sql: |
  select 
  	person.*,
  	person.id as link 
  from person
  where id = ${id}

mappings:

  id:
    mapTo: id
  firstname:
    mapTo: firstname
  lastname:
    mapTo: lastname
  link:
    mapTo: link
    extractWith: "org.evolvis.veraweb.export.CsvExporterTest$MyColumn"