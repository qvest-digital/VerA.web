You only need to install the *.resource files.

The other files contained herein are this README
and a couple of scripts people may find useful
when they edit translations. They are included
in the files tarball only for completeness and
can safely be ignored by end users.
