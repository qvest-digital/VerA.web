<?xml version="1.0" encoding="ISO-8859-1" standalone="no"?>
<!DOCTYPE web-app 
    PUBLIC "-//Sun Microsystems, Inc.//DTD Web Application 2.3//EN" 
    "http://java.sun.com/dtd/web-app_2_3.dtd">

<web-app>

  <display-name>Octopus module</display-name>
  <description>
    tarent-octopus module
  </description>

  <resource-env-ref>
    <description>
      Object factory for octopus context access.
    </description>
    <resource-env-ref-name>
      octopus/References
    </resource-env-ref-name>
    <resource-env-ref-type>
      de.tarent.octopus.jndi.OctopusReferences
    </resource-env-ref-type>
  </resource-env-ref>

<!--
  <servlet>
    <servlet-name>OctopusServlet</servlet-name>
    <servlet-class>de.tarent.octopus.request.servlet.OctopusServlet</servlet-class>

    <init-param>
-->
      <!-- Loglevel for Octopus Loggings. Allowed are log levels from java.util.logging.Level -->
<!--
      <param-name>logging.level</param-name>
      <param-value>WARNING</param-value>
    </init-param>
-->
    <!-- Pfadangaben müssen alle relativ zu '/WEB-INF/' Verzeichniss angegeben werden
         und mit einem '/' enden.
     -->
<!-- 
    <init-param>
      <param-name>paths.logfile</param-name>
      <param-value>log/octopus-%g_%u.log</param-value>
    </init-param>

    <init-param>
      <param-name>paths.configRoot</param-name>
      <param-value>octopus-config/</param-value>
    </init-param>

    <init-param>
-->
      <!-- relativ zum Config-Root aus 'paths.configRoot' -->
<!--
      <param-name>paths.configFile</param-name>
      <param-value>octopus-server.xml</param-value>
    </init-param>

	<load-on-startup>10</load-on-startup>
  </servlet>
 -->


  <servlet>
    <servlet-name>Forwarder</servlet-name>
    <servlet-class>de.tarent.octopus.request.servlet.Forward</servlet-class>

    <!--
      Parameter zum Weiterleiten an eine Octopus Instanz:
      Wenn der Octopus in der gleichen Webanwendung liegt,
      am Besten auskommeniert lassen, 
      damit die Defaults (/<aktuellesModul>/octopus) verwendet werden.
      <init-param>
        <param-name>targetContext</param-name>
        <param-value>/octopus</param-value>
      </init-param>
      <init-param>
        <param-name>targetPath</param-name>
        <param-value></param-value>
      </init-param>
     -->

    <load-on-startup>1</load-on-startup>
  </servlet>

  <servlet-mapping>
    <servlet-name>Forwarder</servlet-name>
    <url-pattern>/do/*</url-pattern>
  </servlet-mapping>

  <servlet-mapping>
    <servlet-name>Forwarder</servlet-name>
    <url-pattern>/OCTOPUS/*</url-pattern>
  </servlet-mapping>

<!--
  <servlet-mapping>
    <servlet-name>OctopusServlet</servlet-name>
    <url-pattern>/octopus/*</url-pattern>
  </servlet-mapping>
 -->

</web-app>