##Add whitespace to values to cut them
#set($eventnamecut = $event.eventname.replaceAll("(.{40})", "$1 "))

<div class="contentBoxColored grayBorder" id="eventTop">
	<div class="table width100">
		<div class="tableRow">
			<div class="tableCell width30 paddingBottom15">
				<strong>Veranstaltungs-ID:</strong> $!message.text($event.id)
			</div>
			<div class="tableCell width30">
				<strong>Beginn:</strong> $!message.date("DATE", $event.begin) #if($event-beginhastime)um $!message.date("TIME", $event.begin) Uhr#end
			</div>
			#if($completeEventHeader)
			<div class="tableCell width30">
				<strong>Einladungsart:</strong>
				#if ($event.invitationtype == 2)
					Ohne Partner
				#elseif ($event.invitationtype == 3)
					Nur Partner
				#else
					Mit Partner
				#end
			</div>
			#end
		</div>
		<div class="tableRow">
			<div class="tableCell width30">
				<strong style="vertical-align: inherit;">Veranstaltungsname:</strong> <pre>$!message.textarea($eventnamecut)</pre>
			</div>
			<div class="tableCell width30">
				<strong>Ende:</strong> $!message.date("DATE", $event.end) #if($event-endhastime)um $!message.date("TIME", $event.end) Uhr#end
			</div>
		</div>
		<div class="tableRow">
			#if($online-registration.activated)
				<div class="tableCell width30">
					<strong style="vertical-align: inherit;">Veranstaltungs-URL:</strong> <pre>$!message.textarea($eventUrl)</pre>
				</div>
			#end
		</div>
		<div class="tableCell width50">
			#if($online-registration.activated)
				<strong>URL für Pressevertreter:</strong><br/>
				#if($pressevertreterUrl && $pressevertreterUrl != "" && $event.mediarepresentatives && $event.mediarepresentatives != "f")
					$pressevertreterUrl
				#end
			#end
			<strong style="vertical-align: inherit;">Max. Anzahl G&auml;ste:</strong> $!message.text($event.maxguest)<br/>
			<strong style="vertical-align: inherit;">Max. L&auml;nge Warteliste</strong> $!message.textarea($event.maxreserve)<br/>
		</div>
	</div>
</div>
